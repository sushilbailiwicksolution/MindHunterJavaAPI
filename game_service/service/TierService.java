package com.bailiwick.game_service.service;

import org.springframework.stereotype.Service;

@Service
public class TierService {/*

	private Logger logger = (Logger) LogManager.getLogger(getClass());

	@Autowired
	DaoTierService daoService;

	public List<? extends GameInterface> getTiers(ReqGameDetails reqGameDetails) {
		try {
			if(reqGameDetails.getGameId() == null) {
				List<ErrorModel> errorModels = new ArrayList<>();  
				errorModels.add(new ErrorModel().setErrorMessage("requested gameId is null."));
				return errorModels;
			}else {
				return daoService.getTiers(reqGameDetails);
			}
		}catch (Exception e) {
			logger.error(e.getMessage(), e);
			return new ArrayList<>();
		}
	}
*/}
